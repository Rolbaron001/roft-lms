/**
 * The declaration of authenticity a learner accepts before a summative is
 * handed in, where the assessment sets no wording of its own.
 *
 * In a file that imports nothing, because the form that shows it runs in the
 * learner's browser and the code that records it runs on the server, and both
 * must use the same words: the wording is copied onto the submission, so what
 * a moderator reads later is what the learner agreed to.
 */
export const DEFAULT_DECLARATION =
  "This is my own work. I have not copied it from anyone else, I have not used " +
  "generative AI to produce it, and I have credited every source I drew on.";
